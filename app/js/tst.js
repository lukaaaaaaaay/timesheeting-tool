(function (window) {
    'use strict';

    var tst = window.tst = window.tst || {};

    tst.modules = {
        app: {
            url: 'http://localhost:1337',
            name: 'tst'
        }
    };
}(window));